---
title: "project_code"
output: html_document
---

```{r}
library(tidyverse)
library(janitor)
library(readr)
library(ggplot2)
library(dplyr)
```

```{r}
sheet1 <- read_csv("data/Microbes data - 2023.csv")
```

```{r}
sheet2 <- read_csv("data/Microbes data - 2024.csv")
```

```{r}
sheet3 <- read_csv("data/Microbes data - 2026.csv")
```

```{r}
clean1 <- sheet1 %>% clean_names() %>%
  mutate(media_na_or_sda=as.factor(media_na_or_sda),
         inside_or_outside=as.factor(inside_or_outside),
         exposure_duration_min=as.factor(exposure_duration_min),
         section=as.factor(section),
         media_additive_none_cyc_or_amp=as.factor(media_additive_none_cyc_or_amp)) %>%
  mutate(media_na_or_sda= recode(media_na_or_sda,
                                   "NA"="NUA",
                                   "SDA"="SDA")) %>%
  mutate(year = "2023") %>% #added to mark year of data - Alan 
  select(-"group_name",-"notes")
```

```{r}
clean2 <-  sheet2 %>% clean_names() %>%
  mutate(media_na_or_sda=as.factor(media_na_or_sda),
         inside_or_outside=as.factor(inside_or_outside),
         exposure_duration_min=as.factor(exposure_duration_min),
         section=as.factor(section),
         media_additive_none_cyc_or_amp=as.factor(media_additive_none_cyc_or_amp)) %>%
  mutate(media_na_or_sda= recode(media_na_or_sda,
                                   "NA"="NUA",
                                   "SDA"="SDA")) %>%
  mutate(media_additive_none_cyc_or_amp= recode(media_additive_none_cyc_or_amp,
                                   "ampicillin"="amp",
                                   "Cycloheximide 0.0004%"= "cyc",
                                   "AMP 50ug"="amp",
                                   "AMP 50 ug"="amp")) %>%
  mutate(year = "2024") %>% #added to mark year of data - Alan 
  select(-"group_name",-"notes")
```


```{r}
clean3 <- sheet3 %>% clean_names() %>%
  mutate(media_na_or_sda=as.factor(media_na_or_sda),
         inside_or_outside=as.factor(inside_or_outside),
         exposure_duration_min=as.factor(exposure_duration_min),
         section=as.factor(section),
         media_additive_none_cyc_or_amp=as.factor(media_additive_none_cyc_or_amp)) %>%
  mutate(media_na_or_sda= recode(media_na_or_sda,
                                   "NA"="NUA",
                                   "SDA"="SDA")) %>%
  mutate(media_additive_none_cyc_or_amp= recode(media_additive_none_cyc_or_amp,
                                   "CHX"="cyc",
                                   "Amp"= "amp",
                                   "AMP"="amp",
                                   "AMP 50 ug"="amp")) %>%
  mutate(inside_or_outside= recode(inside_or_outside,
                                   "Inside"="inside",
                                   "inside"="inside",
                                   "Outside"="outside",
                                   "outside"="outside")) %>%
  mutate(across(where(is.list), as.double)) %>%
  rename( "total_number_colonies" = "total_number_colonies_both_mold_non_mold_colonies_cumulative_counts_from_both_days") %>%  # - Alan
  mutate(year = "2026") %>% #added to mark year of data - Alan 
  select(-"group_name",-"notes",-"name_of_the_data_recorder") #removed names of data recorders - Alan 
  #mutate(number_mold_looking_colonies_have_hyphae=recode(number_mold_looking_colonies_have_hyphae,
                                               #          "NA"=0))
#do a mutate string replace or if else to replace o=0 in hyphae column (currently an NA on clean3) someone fucked up in my class swear to god not me.
```

```{r}
test <- list(clean1, clean2, clean3)
```
```{r}
test2 <- test %>% 
  reduce(full_join, by = "section", "media_na_or_sda", "media_additive_none_cyc_or_amp", "inside_or_outside", "exposure_duration_min",
"days_of_incubation", "number_mold_looking_colonies_have_hyphae", "number_non_mold_looking_colonies_e_g_no_hyphae", "total_number_colonies",
"year")
```


